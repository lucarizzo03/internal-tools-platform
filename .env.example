# Needed only when running the app outside docker compose
# (e.g. `npm run dev` against a local Postgres).
# In compose, the app container gets DATABASE_URL automatically.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/platform

# Postgres container settings (compose defaults shown; override optional).
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=platform
